'use client';
import MenuOverflow from '../menu/MenuOverflow';
import { TableCell } from './TableCell';

export const ActionCell = () => {
  return (
    <TableCell>
      <MenuOverflow />
    </TableCell>
  );
};
